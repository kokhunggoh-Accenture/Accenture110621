import { ValidateEmptyDirective } from './validate-empty.directive';

describe('ValidateEmptyDirective', () => {
  it('should create an instance', () => {
    const directive = new ValidateEmptyDirective();
    expect(directive).toBeTruthy();
  });
});
